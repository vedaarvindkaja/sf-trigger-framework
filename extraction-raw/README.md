# Raw Extraction — LOCAL ONLY

This folder is git-ignored on purpose. Raw config pulled from the client dev org lands here.
**Never commit anything in here.** Genericize first, then write the abstracted version into /docs.
