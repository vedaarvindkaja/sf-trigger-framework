<?xml version="1.0" encoding="UTF-8"?>
<CustomObject xmlns="http://soap.sforce.com/2006/04/metadata">
    <deploymentStatus>Deployed</deploymentStatus>
    <description>Carries error details out of a failing transaction. publishBehavior=PublishImmediately so the event fires the instant EventBus.publish() is called, regardless of whether the originating DML later rolls back. This is what lets the subscriber write an Error_Log__c even for a rolled-back transaction.</description>
    <eventType>HighVolume</eventType>
    <label>Error Event</label>
    <pluralLabel>Error Events</pluralLabel>
    <publishBehavior>PublishImmediately</publishBehavior>
</CustomObject>
